package eco

import (
	"bytes"
	"fmt"
	"math/rand"
	"os"
	"reflect"
	"testing"
	"text/template"
	"time"

	"github.com/dylt-dev/dylt/common"
	"github.com/stretchr/testify/require"
)

func TestGenDecodeDeepTest(t *testing.T) {
	ctx := common.NewEcoContext(os.Stdout)
	types := []reflect.Type {
		{{ range .typeDeclarations }}
		reflect.TypeFor[{{.}}](),
		{{ end }}
	}
	
	r := rand.NewSource(time.Now().UTC().UnixNano())

	type TestInfo struct {
		TypeDeclaration string
		ExpectedVal any
		Depth int
		LastIndex int
		ValueTree string
		ValueRef string
	}

	depth := {{ .depth }} 
	data := []TestInfo{}
	for _, rt := range types {
		values := []any{}
		
		// type declaration
		decl := common.GetDeclFromType(rt)
		
		dt := common.NewDeepType(rt)
		var n int
		common.GenScalarValues(ctx, rt, r, &values)	

		// value tree
		bbTree := bytes.Buffer{}
		dt.EmitTreeDecl(&n, values, &bbTree)
		sValueTree := bbTree.String()

		// value ref
		bbRef := bytes.Buffer{}
		dt.EmitValueRef(values, &bbRef)
		sValueRef := bbRef.String()

		lastVal := values[len(values)-1]
		var sExpectedVal string
		if reflect.ValueOf(lastVal).Kind() == reflect.String {
			sExpectedVal = fmt.Sprintf("%q", lastVal)
		} else {
			sExpectedVal = fmt.Sprintf("%v", lastVal)
		}

		testInfo := TestInfo{
			Depth: depth,
			ExpectedVal: sExpectedVal,
			LastIndex: depth-1,
			TypeDeclaration: decl,
			ValueRef: sValueRef,
			ValueTree: sValueTree,
		}
		data = append(data, testInfo)
	}

	w, err := os.Create("./decode_genned_test.go")
	require.NoError(t, err)
	buf, err := content.ReadFile("content/GenDeepTests.tmpl")
	require.NoError(t, err)
	require.NotNil(t, buf)
	tmpl, err := template.New("deeptest").Parse(string(buf))
	require.NoError(t, err)
	tmpl.Execute(w, data)
	err = w.Close ()
	require.NoError(t, err)
}